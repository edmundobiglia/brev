import React from "react";
import Icon from "./icons/Icon";

const Loader = () => {
    return (
        <div className="loader">
            <Icon iconName="loader" className="loader__icon" />
        </div>
    );
};

export default Loader;
